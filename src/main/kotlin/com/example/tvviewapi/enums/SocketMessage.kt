package com.example.tvviewapi.enums

enum class SocketMessage(val value: String) {
      SLIDE("slides") ,
      REMINDER("reminders"),
      CALENDAR("calendar"),
      TRAIN("train"),
      BUS("bus"),
      BRIDGE("bridge"),
}